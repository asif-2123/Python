import numpy as np

arrr = np.random.rand(10)

print(arrr) # original array
print(np.std(arrr)) # standard deviation of the array
print(np.mean(arrr)) # mean of the array
print(np.median(arrr)) # median of the array